---
import Layout from "./PageLayout.astro"
export interface Props {
	title: string;
}

const { title } = Astro.props.frontmatter;

const realTitle = "CCA - " + title;
---
<Layout title={realTitle}>
    <slot></slot>
</Layout>
